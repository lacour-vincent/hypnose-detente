<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="alertDialogTheme">@style/AppTheme_Dialog</item>
    </style>

    <style name="AppTheme.Toolbar">
        <item name="android:background">@color/colorToolbarBackground</item>
        <item name="android:textColorPrimary">@color/colorToolbarText</item>
        <item name="colorControlNormal">@color/colorToolbarText</item>
    </style>

    <style name="AppTheme.Toolbar.Transparent">
        <item name="android:background">@null</item>
        <item name="android:textColorPrimary">@color/colorToolbarText</item>
        <item name="colorControlNormal">@color/colorToolbarText</item>
    </style>

    <style name="AppTheme.Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">16sp</item>
    </style>

    <style name="AppTheme.Toolbar.PopUp" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColor">@color/colorDialogContent</item>
        <item name="android:background">@color/colorDialogBackground</item>
    </style>

    <style name="AppTheme_Dialog" parent="Theme.AppCompat.DayNight.Dialog">
        <item name="android:textColor">@color/colorDialogTitle</item>
        <item name="android:background">@color/colorDialogBackground</item>
    </style>

    <style name="AppTheme_Loading_Dialog" parent="Theme.AppCompat.Dialog">
        <item name="android:textColor">@color/colorDialogTitle</item>
        <item name="android:textColorPrimary">@color/colorDialogTitle</item>
        <item name="android:background">@color/colorDialogBackground</item>
    </style>

</resources>
