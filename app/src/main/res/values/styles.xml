<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:colorEdgeEffect">@color/colorPrimary</item>
    </style>

    <style name="ToolBarTheme" parent="AppTheme">
        <!-- Base application thème. -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="NewDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:titleTextColor">@color/ColorBGToolbar</item>
        <item name="android:textColorPrimary">@color/ColorBGToolbar</item>
    </style>

</resources>
